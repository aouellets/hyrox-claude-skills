# Example 02 — Incomplete data: partial coverage, missing baselines

## Request

> How's my progress? Wall balls are now 5:50 and my fresh 1k is 4:28. I think the
> block went okay. That's about all I've tracked.

## Response (abridged)

This is a **provisional** report — I can only comment on two areas, and for one of
them I don't have a baseline, so I won't manufacture a trend.

**Progress table (what I can report):**

| Area | Baseline → current | Delta | Read |
|---|---|---|---|
| Fresh 1k | unknown → 4:28 | **no comparison — needs baseline** | one data point; not a trend |
| Wall balls (100) | unknown → 5:50 | **no comparison — needs baseline** | 5:50 is a respectable mark, but I can't say if it improved |
| Compromised running | no data | — | the block's likely focus, untracked |
| Stations (other 7) | no data | — | no read |
| Simulations / strength | no data | — | no read |
| Adherence / recovery | not given | — | can't separate effect from execution |

**Honest read.** With no prior baselines I can't tell you whether the block
"worked" — these are snapshots, not progress. I won't invent a week-0 number to
fill the table.

**The two fields that would change this most:** your **prior** wall-ball and
compromised-run values (so I can compute deltas), and adherence (so I can tell
"didn't work" from "wasn't done"). A strength PB or bodyweight won't move the
priorities much right now.

**Routing.** If you have no baselines at all, this is really an **assessment**, not
a progress report — route to `hyrox-athlete-assessment` for a ranked limiter read
from current numbers. Going forward, standardize a small retest set
(`hyrox-benchmark-and-testing-builder`) so the next report has real deltas.
